from random import randint

class Chromosome:
   self.fitness = 0
   self.chromo = []

   def  CreateRandomChromosome(self):
        for i in len(chromo):
            chrom[i] = randint(0,1)

   def  SetChromosome(self, X):
       for i in len(chromo):
            chromo[i] = X[i]

   def  printMe(self):
       print "chromo:"
       for i in len(chromo) :
           print chromo[i]
       print "|fitness:%s".format( self.fitness)


myChromo = Chromosome()

myChromo.printMe()

